pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenLocal()
        // maven(url="https://dl.bintray.com/kotlin/dokka")
    }
}

rootProject.name = "gradle-yapp-publisher-plugin"

